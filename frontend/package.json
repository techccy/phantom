{
  "name": "phantom-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && vite build --config vite.sdk.config.ts",
    "build:app": "tsc --noEmit && vite build",
    "build:sdk": "tsc --noEmit && vite build --config vite.sdk.config.ts",
    "preview": "vite preview",
    "test:interop": "node scripts/interop-test.mjs",
    "test:fallback": "npx tsx scripts/e2e-fallback-test.ts"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vite-plugin-javascript-obfuscator": "^3.1.0"
  },
  "dependencies": {
    "@noble/ciphers": "^2.2.0",
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0"
  }
}
